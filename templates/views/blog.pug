extends ../layouts/default

block content
	section.blog
		if blog.length
			each post in blog
				a.post(href="/blog/" + post.slug)
					strong= post.title

	.popup
		//- .popup-overlay
		button.close-popup
		.popup-content

block js