extends ../layouts/default
		
block content
	section.contact
		h1 Contact
		#map
		form.form(method='post' onsubmit='return validateForm(this);' autocomplete='on')
			input#action(type='hidden', name='action', value='contact')
			label(for='name') Name
			input(type='text', name='name', placeholder='Nom')
			label(for='email') Email
			input(type='email', name='email', placeholder='Email')
			label(for='phone') Phone
			input(type='text', name='phone', placeholder='Téléphone (optionnel)')
			label(for='enquiryType') Sujet
			select(name='enquiryType')
				option(value='') (sélectionnez un sujet)
				each type in enquiryTypes
					option(value=type.value)= type.label
				label(for='message') Message
			textarea(name='message', placeholder='Laissez-nous un message…')= formData.message
			button(type='submit')
				span.button-text Send
				span.mail-loader
			.result-box(aria-hidden="true")
				if responses.success
					p.result-message.success Votre message a été envoyé, merci.
				if responses.invalidEmail
					p.result-message.error Erreur&nbsp;: Veuillez entrer une adresse email valide.
				if responses.missingFields
					p.result-message.error Erreur&nbsp;: Des champs requis n'ont pas été remplis
				if responses.invalidCaracters
					p.result-message.error Erreur&nbsp;: L’email contient des caractères invalides.
				if responses.failure
					p.result-message.failure Erreur&nbsp;: Votre message n’a pas pu être envoyé, veuillez réessayer plus tard.

block js
	// GOOGLE MAPS
	script
		| function initMap() {
		| 	var uluru = {lat: 47.226, lng: -1.556};
		| 	var map = new google.maps.Map(document.getElementById('map'), {
		| 		zoom: 16,
		| 		center: uluru
		| 	});
		| 	var marker = new google.maps.Marker({
		| 		position: uluru,
		| 		map: map
		| 	});
		| }
	script(async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCX80NEyVT-3xGIrv1GEZkUrtHdi-giF3I&callback=initMap")

	// MAPBOX
	//- script(src='https://api.mapbox.com/mapbox-gl-js/v0.38.0/mapbox-gl.js')
	//- script
	//- 	| mapboxgl.accessToken = 'pk.eyJ1IjoibGUtYmllbnRvdC1ub21tZSIsImEiOiJjajZlMjZhM3AwY3RyMzNvNGJ6cHY2bzE3In0.Anxup6TH3PiVlpoFU0NW6A';
	//- 	| var map = new mapboxgl.Map({
	//- 	| container: 'map',
	//- 	| style: 'mapbox://styles/mapbox/dark-v9'
	//- 	| });